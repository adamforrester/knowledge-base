You are researching generated-from-data documentation as a commercial default for design-system engagements — the architecture that treats the component data file as the source of truth and derives the documentation, the AI-readable metadata, the Figma library properties, and the conformance evidence from it — for a senior design systems practitioner at a digital agency. The output is a structured knowledge document.

Context: the practice has the architectural position articulated. 04-documentation §3–§4 covers the four-layer AI documentation stack and the case for parallel human-and-machine layers. 03-component-library covers components-as-data as the substrate. 15-ai-in-ds covers MCP-served metadata and the four-prerequisite minimum. 29-per-component-documentation-template covers the per-component template, the generation-vs-authoring seam, the CI freshness check, and the version-zero YAML schema. What the practice does not yet have is a coherent treatment of *how to actually ship generated-from-data documentation as a commercial default* — the upfront engineering investment, the tooling stack as it actually exists in 2026, the migration path for systems already on authored prose, the per-component hours-and-cost shift this produces, the decision framework for when generation is justified vs. when authored prose is enough, and the commercial pitch that converts this from internal practice IP into a sellable scope item.

The gap this fills: 09 §1.10 names "generated-from-data documentation not yet default; practice ships authored prose" as the residual gap after the per-component template was closed by 29. 09 §1.12 names "AI-readiness as commercial default" as a sibling gap — `.ai.json` is in the AI-Compatible doc but not the proposal; AGENTS.md / CLAUDE.md is unique IP not yet shipped by default; Code Connect is not defaulted. These three sit in a cluster: the architecture is in the vault, the proposal language isn't. This research closes the documentation strand of that cluster and gives the practice a defensible commercial position to ship in the next engagement.

This is not an introductory documentation guide — assume the reader knows what a design system is, what Storybook / MDX / `.ai.json` / MCP / Code Connect are, what components-as-data means, what the four-audience problem is per 29, and what the practice already encodes about documentation strategy. Focus on the *commercial-default* angle — the engineering investment, the tooling realities, the migration playbook, the cost shift, the pitch. Where decisions are context-dependent, give a decision framework rather than declaring a winner. Where the practice can claim defensible POV that external sources don't articulate, surface it. Where the field is honestly behind ("we can theoretically generate this, we don't yet ship it generated by default" per 29 §5), name the gap and the practitioner workaround.

Research scope — cover all of the following. Use these as the section spine of the output:

1. The architecture in production — what generated-from-data documentation actually looks like in 2026

   - The component-data-file shape: TypeScript type as the implicit source vs. an explicit JSON Schema / YAML definition vs. a framework-native definition (Lit web component, Stencil, plain React with Zod, plain Svelte with `<script lang="ts">` types). Trade-offs per choice; what each enables and forbids downstream.
   - The generation targets: Storybook autodocs, MDX docs site, `.ai.json` registry, Figma library properties, AI assistant catalog (component cards, RAG-prepared chunks). What each consumer needs from the source and where the projections diverge.
   - The CI surface: freshness check (per 29 §5), schema validation, accessibility schema enforcement, link verification, screenshot regeneration, semver bump detection, deprecation propagation across all consumers.
   - The single-source-of-truth file structure (the `Button.tsx` / `.types.ts` / `.stories.tsx` / `.docs.mdx` / `.ai.json` / `.a11y.json` / `.tokens.ts` / `.test.tsx` shape from 29) — what each file owns, what they share, where authoring happens, where overlay happens.
   - The seam where authoring still happens — purpose, when-to-use, avoid-when, do/don't, content guidelines, the accessibility "consumer must specify" rows. The discipline that authored prose remains authored even when the surrounding facts are generated.

2. The pipeline shape — the concrete dependency graph

   - From source change to each consumer's update. What runs when, what blocks what, what fails build vs. warns.
   - The Storybook autodocs path: docgen extracting types and JSDoc; controls panel as authoring surface; the MDX overlay; addon-a11y running axe-core; Chromatic for visual regression.
   - The MDX docs site path: pulling type signatures, default values, deprecations from the source; rendering authored prose alongside; resolving cross-references between components.
   - The `.ai.json` emission path: schema generation from types and JSDoc + `@ai` tags; authored fields (`when_to_use`, `avoid_when`, `business_context`, `semantic_prompt`); the MCP server that serves it; the AI assistant's RAG ingestion.
   - The Figma library sync path: Code Connect mapping component-side props to Figma component properties; the unidirectional vs. bidirectional question; the Figma sync cadence and where drift creeps in.
   - The conformance dossier path: per-component automated test results from CI; manual-AT walkthrough log links; SC-by-SC mapping; aggregation into the consuming product's ACR.
   - The per-pipeline failure mode — what does practitioners encounter when a step in the chain breaks and how the system recovers vs. silently drifts.

3. The hours-and-cost shift — what changes commercially when generation replaces authoring for the technical surface

   - The upfront engineering investment: docgen tooling, MDX template, `.ai.json` schema, CI freshness check, per-section minimum-prose rules, Storybook autodocs config, MCP server scaffolding. Realistic 2026 numbers — 40–80 hours? More? Less? Per platform / framework / hosted-vs-self-hosted.
   - The per-component shift: how 29's "3–8 hours per component for UX Designer authored sections" changes when the props table, type signatures, default values, deprecation flags, version-introduced metadata, and default examples come from the source. The expected redistribution between UX Designer (authored intent) and Engineering (data file) hours.
   - The break-even component count: at what system size does the upfront pipeline investment pay back vs. authored prose? 30 components? 60? 100?
   - The lifetime benefit: how the cost curve diverges over the 18–36 month post-handoff window where authored prose silently drifts and generated docs stay aligned.
   - How the practice itemises this in the SOW vs. how it currently absorbs it as overhead.
   - The commercial discipline of treating the pipeline as a separate Foundations-tier deliverable, not a Documentation sub-bullet.

4. The decision framework — when is generation justified, when is authored prose enough

   - Component count threshold (the "30+ components" rule, with exceptions).
   - Consumer-product count (one consumer team is different from twelve).
   - System lifetime expectation (2-year retainer is different from 6-month build-and-handoff).
   - Engineering culture (engineering-resident clients vs. design-led organisations where the docs author is not a developer).
   - Multi-platform reach (web-only is simpler than web + iOS + Android).
   - Multi-brand requirement (single-brand is simpler than 5-brand portfolio per 24-tokens-at-scale).
   - AI-readiness procurement requirement (clients explicitly asking for AI-tool integration vs. clients indifferent).
   - Regulatory constraint (EAA / 508 / HIPAA pushes toward conformance dossier aggregation, which assumes generation).
   - The framework: a 5-question decision tree the practice can apply in the proposal-shaping conversation.

5. Migration paths — the playbook for moving an existing authored-prose docs site onto a generated substrate

   - The most common starting points: Notion / Confluence / GitBook / Zeroheight / Storybook-without-autodocs / hand-rolled-docs-site.
   - The two-phase migration: scaffold the pipeline alongside the existing site, dual-publish for a release window, retire the old site. Realistic timeline per starting point.
   - What to throw away: stale authored prose, broken examples, deprecated screenshots, redundant prop documentation that contradicts the source.
   - What to keep: purpose / when-to-use / avoid-when / do-don't / content-guidelines authored content. The seam between "the pipeline can generate this" and "this is irreplaceable editorial content."
   - The "we still want Zeroheight for designers" reality — how to run hosted design-side surface synced from the same source; the cadence and the failure modes when sync breaks.
   - The migration anti-patterns: rebuilding the site without the editorial content, throwing away the existing prose because it's hand-authored, treating migration as engineering-only when it is half editorial.

6. The cluster relationship — how generated-from-data documentation relates to the broader AI-readiness commercial default

   - The four-prerequisite minimum architecture (per 15-ai-in-ds and 25-ai-aware-patterns-and-conversational-ui): tokens-with-descriptions, component metadata (`.ai.json` + registry), agent instructions (AGENTS.md / CLAUDE.md), Figma pipeline (variables → DTCG → Style Dictionary → Code Connect → MCP).
   - Generated documentation is the *delivery surface* of the four-prerequisite architecture for the docs consumer. The AI-readable layer is the same artefact at a different altitude.
   - Code Connect not defaulted (09 §1.7) sits in the same cluster — Figma↔code mapping that AI agents consume.
   - Custom MCP server / registry not commercial (09 §1.7) — the practice has the IP; not yet sold.
   - AGENTS.md / CLAUDE.md as unique practice IP — the instruction-file layer no external source names; commercial proposals don't ship it.
   - The pitch: the practice's offer is the *whole cluster*, not just the docs site.

7. The commercial pitch — how the practice articulates this to clients who haven't asked for it

   - The procurement framing: "AI-ready design system" is the surface clients increasingly ask for; the practice's specific articulation is the four-prerequisite architecture with generated documentation as the delivery surface.
   - The cost narrative: "the upfront pipeline investment is a one-time cost that pays back across all 60 components and the system's lifetime; authored prose carries an ongoing drift cost and an AI-readiness gap that is increasingly procurement-grade."
   - The risk narrative: "without generation, the docs and the implementation drift; the conformance dossier is reverse-engineered every audit; the AI tools your team uses cannot reliably reach the system."
   - The competitive narrative: "vendor templates ship structure; the practice ships the architecture, the editorial discipline, and the cluster of artefacts that make the structure load-bearing."
   - The phased adoption: clients who can't take the full pipeline at engagement start can adopt incrementally — Storybook autodocs first, `.ai.json` registry second, MCP server third.
   - The objection handling: clients who say "we just want a docs site" — what the practice's response is, when the practice walks away, when the practice scopes the smaller deliverable explicitly.

8. Anti-patterns and tooling realities — where the field is genuinely behind in 2026

   - "Storybook autodocs alone is enough" — what autodocs ships and what it doesn't, where the gap shows up.
   - "Generate everything including intent" — the failure mode where the LLM authors purpose / when-to-use prose and the result is generic and wrong.
   - The complex-multi-variable-state cliff (per 29 §5) — automated state rendering for `selected + focused + server-error` cells is fragile; the practitioner workaround.
   - The Figma sync seam — design-side libraries don't yet pull from the same source automatically; the design library is rebuilt on a separate cadence and drift creeps in.
   - The `.ai.json` schema is internal-tier IP — no canonical schema in the field; the practice can claim it but should publish it to establish the layer.
   - Zeroheight's API for generated content has matured but remains constraining; for hosted-design-side users, the practice treats Zeroheight as the consumer surface and code-side docs as the source.
   - The authoring-tooling gap: most authoring tools (Notion, Confluence, GitBook) cannot consume the generated source at all and will need to be replaced or run in parallel.
   - The Code Connect 2025–2026 maturity question: Code Connect is now Baseline-grade for React; iOS / Android / Flutter Code Connect is materially less mature; cross-platform systems hit the asymmetry.
   - The MCP server tooling reality: building a custom DS MCP server in 2026 is a 40–80-hour project; the field does not yet have a turnkey "DS MCP server" the practice can ship as a vendor selection.

Output format: Structured markdown using the numbered scope as section headings. Synthesize — do not list sources mechanically. Where the field has clear consensus or one obvious choice (e.g., "the props table is generated, period"), state it; where decisions are context-dependent, give a decision framework rather than declaring a winner. Flag tooling gaps that practitioners work around. Where claims depend on current platform state — Storybook version, MDX syntax, MCP server tooling, Code Connect platform support, vendor product features — date them and note the verification path. Include a references section linking to primary sources (Storybook docs, react-docgen-typescript, MDX docs, Curtis Components-as-Data, Wolosin metadata loop, Couldwell Laying the Foundations, Mangialardi Design Systems for Developers, Code Connect docs, MCP specification, Zeroheight / Supernova / Knapsack docs, public exemplars where the architecture is partially visible — Atlassian, Adobe Spectrum, Carbon, Polaris, Material Web).

Depth: Expert audience. Do not explain what Storybook or MDX is — explain what it takes to ship generated-from-data documentation as a default deliverable, where the field is genuinely behind in 2026, and what the practice should commit to as a commercial position that converts the architectural articulation in 04 / 15 / 29 into the SOW.
